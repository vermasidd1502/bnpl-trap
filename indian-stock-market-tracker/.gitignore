groww-secrets/
__pycache__/
*.pyc
.env
*.output

# runtime logs (detached server / guardian / recorder)
*.log
*.err

# account-specific runtime state — NEVER push (contains live positions / P&L / watchlist)
_var_book.json
marleg_guardian_state.json
marleg_tick_store.json
marleg_tick_focus.json
marleg_gtt_plan.json
marleg_buy_plan.json
marleg_engine.json
marleg_engine_trades.csv
marleg_autotrader_book.json
marleg_gatebot_book.json
token_cache.json
TOTP_SETUP_GUIDE.txt

# big local datastore
*.duckdb
*.duckdb.wal

# more account-specific runtime state
marleg_paper_book.json
my_positions.json
marleg_paper_*.json
marleg_stop_check.json

# gated-list daily history (runtime-accumulated snapshots)
marleg_gated_history.json

# transition-watch daily state snapshots (runtime)
marleg_transition_history.json

# daily EOD-scan refresh lockfile (runtime)
marleg_eod_running.lock

# fib study output (research artifact, regenerable)
marleg_fib_study.json

# short-gate study output (research artifact, regenerable)
marleg_short_gate_study.json

# hourly gated cache (runtime, regenerated each hour during market hours)
marleg_gated_hourly.json

# mood study output (research artifact, regenerable)
marleg_mood_study.json

# buy-hold durability screen cache (runtime, regenerated by the scan)
marleg_buyhold_cache.json

# fundamentals batch cache (runtime, built incrementally by the scan)
marleg_fundamentals_cache.json

# pattern reliability study output (research artifact, regenerable)
marleg_pattern_study.json
marleg_breakout_timing_study.json

# pattern scan cache + smart-stop study output (runtime/research, regenerable)
marleg_pattern_scan.json
marleg_smartstop_study.json
*.out

# industry RS cache (runtime, written by the gated scan) + gate study (research artifact)
marleg_industry_rs_cache.json
marleg_industry_gate_study.json

# closing-pressure / square-off study output (research artifact, regenerable)
marleg_closing_pressure_study.json

# industry-momentum rotation backtest output (research artifact, regenerable)
marleg_industry_momentum_bt.json

# gate-entry model comparison backtest output (research artifact, regenerable)
marleg_gate_models_bt.json

# canonical cached daily price panel (runtime, regenerable, large)
marleg_panel_cache.pkl

# strategy-lab results (research artifact, regenerable)
marleg_strat_lab.json

# macro-gate backtest output (research artifact, regenerable)
marleg_macro_gate_bt.json

# macro overlay backtest output (research artifact, regenerable)
marleg_macro_overlay_bt.json

# earnings-date cache (runtime, best-effort, regenerable)
marleg_earnings_cache.json

# accumulation-edge backtest output (research artifact, regenerable)
marleg_accum_study.json

# War Room session settings (user's own capital / risk / goal / pinned sectors — local state)
marleg_warroom_config.json

# VIX conscience + driver study, tier-ladder study, high-mover radar (research/runtime, regenerable)
marleg_vix_study.json
marleg_tier_study.json
marleg_movers.json
marleg_lastcandle_study.json
marleg_cuphandle_study.json

# live cup-with-handle radar (runtime, regenerated by the EOD scan)
marleg_cuphandle.json

# sector-rotation backtest + industry persistence/beta (research/runtime, regenerable)
marleg_rotation_bt.json
marleg_industry_persistence.json
marleg_rotation_walkforward.json
