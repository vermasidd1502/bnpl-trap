# Secrets
.env
*.env
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data / warehouse
*.duckdb
*.duckdb.wal
bnpl-pod/data/warehouse.duckdb*
bnpl-pod/logs/**/*.jsonl

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Fonts (large binaries, not needed in repo — users fetch locally)
fonts/

# Local rescue backups (paranoia copies of the working tree; never committed)
bnpl-pod-rescue-backup-*/

# Per-machine Claude workspace config (not project artifacts)
.claude/
bnpl-pod/.claude/

# Design asset drop (kept outside repo to avoid binary bloat)
_design_unpack/
BNPL pod design.zip
